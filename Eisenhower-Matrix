# Eisenhower Matrix: Smart City Transport

| Quadrant | Tasks |
| -------- | ----- |
| **1. Urgent & Important** (Do First) | • **Testing:** Verify `simulateRun()` handles passengers with invalid destinations gracefully.<br>• **GitHub:** Upload `src/main.cpp` immediately.<br>• **Validation:** Fix infinite loop bug if user enters 'abc' instead of a number for menu choice. |
| **2. Important & Not Urgent** (Schedule) | • **Persistence:** Implement file I/O to save/load Stations and Routes so data isn't lost on exit.<br>• **Refactor:** Move `Passenger` struct to a header file. |
| **3. Urgent & Not Important** (Delegate) | • **Styling:** Add more descriptive distinct separators in console output.<br>• **Docs:** Add comments explaining `std::map` usage for the external examiner. |
| **4. Not Urgent & Not Important** (Delete) | • **Circular Queue:** (Completed via `std::queue`).<br>• **Manual Memory Management:** (Obsolete due to STL). |
